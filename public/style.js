var dHeight = $(window).height();

$('#indexContent').height(dHeight);

jQuery(window).load(function(){
	//$('#form-create-game').css('top',(dHeight / 2 )-($('#form-create-game')[0].clientHeight / 2));
	//$('.logoContainer').css('top',dHeight * .07);
});
